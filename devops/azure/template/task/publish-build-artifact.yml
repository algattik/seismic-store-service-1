parameters:
  providerName: ""
steps:
  - task: PublishBuildArtifacts@1
    displayName: 'Publish Artifact'
    inputs:
      PathtoPublish: '$(Build.Repository.LocalPath)/${{ parameters.providerName }}/$(utest.runtime.image).tar'
      ArtifactName: '${{ parameters.providerName }}_$(utest.runtime.image)'
      publishLocation: 'Container'
    condition: succeededOrFailed()